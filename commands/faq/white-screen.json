{
  "title": "White screen after install",
  "footer": "",
  "body": [
    "This is usually caused by the cache folder within the web root (and its subdirectories) not being writable. ",
	"You will need to modify the permissions recursively of the folder so the process running PHP can write to it.\n\n",
	"An example command for Apache and nginx on Ubuntu is\n",
	"``sudo chown -R www-data:www-data /var/www/html/cache``"
  ]
}
